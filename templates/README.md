# Templates

This directory is reserved for future Jinja-style render templates.

The current MVP uses Python render functions directly so that the schema and
output shape can settle before template extraction.

